server:
  port: 8150

  servlet:
    session:
      timeout: 30m

spring:
  application:
    name: lvtumall-auth-server
  cloud:
    nacos:
      discovery:
        server-addr: 39.105.108.186:8848

    sentinel:
      transport:
        #配置sentinel dashboard地址
        dashboard: 39.105.108.186:8858

  redis:
    host: 39.105.108.186
    port: 6379

#开启feign对sentinel的支持
feign:
  sentinel:
    enabled: true


  thymeleaf:
    cache: false

  session:
    store-type: redis

#暴露所有端点
management:
  endpoints:
    web:
      exposure:
        include: '*'



